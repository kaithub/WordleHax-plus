{
  "name": "WordleHax-plus",
  "version": "1.2.1",
  "description": "Extensible modding for Wordle.",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc && webpack --mode=production --node-env=production && node src/fixBuild.js",
    "build:dev": "webpack --mode=development",
    "build:prod": "webpack --mode=production --node-env=production",
    "watch": "webpack --watch"
  },
  "keywords": [],
  "author": "Kai/S",
  "license": "ISC",
  "devDependencies": {
    "@webpack-cli/generators": "^3.0.1",
    "eslint": "^8.36.0",
    "prettier": "^2.8.4",
    "webpack": "^5.76.1",
    "webpack-cli": "^5.0.1"
  },
  "dependencies": {
    "terser": "^5.16.6",
    "ts-loader": "^9.4.2"
  }
}
